{"task": "Exactitud ante todo: PIB sobre valor agregado, La Tierra como briefing útil, y cacería de discrepancias.", "tasks": [{"id": "ac_pib", "thinking": "medium", "filename": "ac_pib.json", "prompt": "Devuelve SOLO JSON crudo: {\"parches\":[{\"buscar\":\"...\",\"reemplazar\":\"...\"}]}. buscar = subcadena EXACTA y ÚNICA; máx 6 parches.\nARCHIVO: simcolombia/pipeline/build_marginals.py. EXACTITUD METODOLÓGICA del bloque PIB:\n(1) 'Impuestos' es la línea de impuestos-menos-subvenciones del PIB, NO una actividad: exclúyela del acumulado (if 'impuestos' in actividad.lower(): continue) para que las participaciones sean sobre VALOR AGREGADO;\n(2) sube top de 6 a 7 actividades;\n(3) fuente nueva exacta: f\"DANE PIB dptal {last} (participación % en valor agregado, precios corrientes · datos.gov.co kgyi-qc7j)\" — 2023 es el último año publicado, verificado hoy.\nCÓDIGO ACTUAL (bloque PIB):\n```\nreturn NAME2COD.get(norm(c))\n\nM = {k: {\"nombre\": v, \"fuentes\": {}} for k, v in DPTOS.items()}\n\n# ── PIB por actividad (kgyi-qc7j): estructura económica → ocupaciones ──\npib = json.loads((RAW / \"pib_departamental.json\").read_text())\nyears = [r.get(\"a_o\") for r in pib if r.get(\"a_o\")]\nlast = max(years)\nacc = defaultdict(lambda: defaultdict(float))\nfor r in pib:\n    if r.get(\"a_o\") != last or \"corrientes\" not in (r.get(\"tipo_de_precios\") or \"\"):\n        continue\n    d = dep(r.get(\"c_digo_departamento_divipola\") or r.get(\"departamento\"))\n    if d:\n        try: acc[d][r.get(\"actividad\",\"?\")] += float(r.get(\"valor_miles_de_millones_de\") or 0)\n        except ValueError: pass\nfor d, sect in acc.items():\n    tot = sum(sect.values()) or 1\n    top = dict(sorted(((k, round(v/tot*100,1)) for k,v in sect.items()),\n                      key=lambda x:-x[1])[:6])\n    M[d][\"pib_sectores_pct\"] = top\n    M[d][\"fuentes\"][\"pib\"] = f\"DANE PIB dptal {last} (datos.gov.co kgyi-qc7j)\"\n\n# ── Salud (23gb-dhmd, municipal → dpto por DIVIPOLA) ──\nsal = json.loads((RAW / \"salud_municipios.json\").read_text())\nyears_s = sorted({r.get(\"periodo\") for r in sal if r.get(\"periodo\")})\nlast_s = years_s[-1]\nagg = defaultdict(lambda: defaultdict(float))\nfor r in sal:\n    if r.get(\"periodo\") != last_s: continue\n    d = (r.get(\"codigo\") or \"\")[:2]\n    if d not in DPTOS: continue\n    for k, col in [(\"contributivo\",\"cobertura_regimen_contributivo\"),\n                   (\"subsidiado\",\"cobertura_regimen_subsidiado\"),\n        \n```"}, {"id": "ac_tierra", "thinking": "high", "filename": "ac_tierra.json", "prompt": "Devuelve SOLO JSON crudo: {\"parches\":[{\"buscar\":\"...\",\"reemplazar\":\"...\"}]}. buscar = subcadena EXACTA y ÚNICA; máx 6 parches.\nARCHIVO: dashboard/sim/index.html. REDISEÑO DE 'LA TIERRA' — de muro de texto a briefing de app útil. Dirección de Daniel: manener personalidad pero serio y útil; lenguaje asertivo de app; orden y espacios curados; EXACTITUD ante todo.\nEstructura nueva del innerHTML (el dossier `dos` trae: apodo_gentilicio, como_hablan, vida_cotidiana, economia_vivida, orgullos[], dolores[], y opcionales):\n1. Cabecera: `<b>{gentilicio}</b>` en línea propia;\n2. Micro-secciones con etiqueta pixel (usa la clase existente h3 o un span nuevo .mh con font Silkscreen 8px color var(--faint) letter-spacing .12em margin '14px 0 6px' display:block):\n   «CÓMO SE HABLA» → como_hablan (recorta a lo esencial, 2-3 líneas máx via CSS: display:-webkit-box;-webkit-line-clamp:3;-webkit-box-orient:vertical;overflow:hidden);\n   «EL DÍA A DÍA» → vida_cotidiana;\n   «DE QUÉ VIVE LA GENTE» → economia_vivida SIN cursiva (la cursiva actual cansa);\n   «LO BUENO» → los cuadros verdes (orgullos) tal cual;\n   «LO QUE PESA» → los cuadros rojos (dolores);\n3. line-height 1.55, párrafos con margin 0 0 8px; nada de <br><br> dobles;\n4. Cierra con `<div class=\"fuente\">dossier del enjambre · verificado por inspector Jev</div>`.\nEl caso nacional (Colombia entera) déjalo como está. Da también el parche CSS de .mh (y .clamp si la usas) para pegar antes de </style>.\nZONA ACTUAL:\n```\ndiv>\n    <div class=\"fuente\">${esc(d.fuentes?.salud||\"\")}</div>`;\n  }\n  const pib=d.pib_sectores_pct;\n  if(pib){\n    h+=`<h3>DE QUÉ VIVE (PIB por sector)</h3>`;\n    const maxP=Math.max(...Object.values(pib))||1;\n    for(const [s,p] of Object.entries(pib))\n      h+=`<div class=\"hbar\"><div class=\"t\"><span>${esc(s.split(';')[0].trim())}</span><b>${p}%</b></div>\n          <div class=\"g\"><i style=\"width:${(p/maxP*100).toFixed(1)}%;background:var(--work)\"></i></div></div>`;\n    h+=`<div class=\"fuente\">${esc(d.fuentes?.pib||\"\")}</div>`;\n  }\n  $(\"#censobody\").innerHTML=h;\n  /* ficha */\n  if(nac){\n    $(\"#dossier\").innerHTML=`<b>Colombia entera en la mesa.</b><br><br>\n      ${(d.poblacion/1e6).toFixed(1)} millones de personas proyectadas por el DANE\n      para 2026, 33 territorios, 6.600 residentes sintéticos. En el panel nacional\n      cada departamento entra a la muestra según su población REAL: pesa más un\n      bogotano que un vaupense, como en el país de verdad.<br><br>\n      <span class=\"tag\" style=\"border-color:var(--ok)\">muestreo proporcional DANE</span>\n      <span class=\"tag\" style=\"border-color:var(--ok)\">33 voces regionales</span>`;\n  } else\n  $(\"#dossier\").innerHTML= dos.vida_cotidiana?\n    `<b>${esc(dos.apodo_gentilicio||d.nombre)}</b> · ${esc(dos.como_hablan||\"\")}<br><br>\n     ${esc(dos.vida_cotidiana)}<br><br><i style=\"color:var(--faint)\">${esc(dos.economia_vivida||\"\")}</i><br><br>\n     ${(dos.orgullos||[]).map(o=>`<span class=\"tag\" style=\"border-color:var(--ok)\">${esc(o)}</span>`).join(\"\")}\n     ${(dos.dolores||[]).map(o=>`<span class=\"tag\" style=\"border-color:var(--fail)\">${esc(o)}</span>`).join(\"\")}`\n    :`<i style=\"color:var(--faint)\">dossier en camino — lo está escribiendo el enjambre</i>`;\n  const mu=$(\"#muestra\");mu.innerHTML=\"\";\n  mine.slice(0,10).forEach(r=>{\n    const div=document.createElement(\"div\");div.className=\"res\";\n    RESBY[r.id]=r;div.dataset.rid=r.id;div.title=\"ver ficha DANE\";\n    const cv=document.createElement(\"canvas\");retrato(cv,r.id,\n```\nEJEMPLO REAL de dossier (Bogotá): {\"apodo_gentilicio\": \"rolo/rola (coloquial), bogotano/bogotana (formal)\", \"como_hablan\": \"Español andino con usted entre desconocidos y 'parce' o 'sumercé' según el barrio; ritmo pausado, diminutivos y muletillas como 'o sea', 'de una', '¿s\", \"vida_cotidiana\": \"Días largos entre TransMilenio, ciclorruta y trancones; se madruga desde las 4 a.m. en Soacha, Kennedy o Suba hacia el centro o el norte. Almuerzo en \", \"economia_vivida\": \"La ciudad vive del comercio, el transporte, los restaurantes, el empleo público, la salud, la educación y los arriendos. Muchos trabajan en ventas, lo\", \"orgullos\": \"['Su sistema masivo de transporte y la ciclovía dominical, referentes en la región', 'La oferta cultural y educativa: museos, bibliotecas, universidad\", \"dolores\": \"['Movilidad lenta y costosa, con trancones y TransMilenio saturado en hora pico', 'Informalidad laboral, inseguridad y arriendos que aprietan el presu\"}"}, {"id": "ac_hunt", "thinking": "high", "filename": "ac_hunt.json", "prompt": "Devuelve SOLO JSON crudo: {\"hallazgos\":[{\"gravedad\":\"alta|media|baja\",\"donde\":\"...\",\"problema\":\"...\",\"fix\":\"...\"}]}. Máx 6, SOLO discrepancias REALES de exactitud de datos.\nAUDITORÍA DE EXACTITUD del panel censo de ColombIA: compara lo que el código MUESTRA contra lo que las fuentes SON. Datos duros verificados hoy: PIB dptal último año publicado = 2023 (datos.gov.co kgyi-qc7j); salud viene de 23gb-dhmd (corte del pipeline); proyecciones de población CNPV2018 post-COVID año 2026; 'Impuestos' no es actividad productiva. Busca: etiquetas que prometan más de lo que el dato es (¿dice 'PIB dptal 2023' pero suma con impuestos?), unidades mal rotuladas, porcentajes que no suman, fuentes desactualizadas o vagas en fichas/censo/dossier, y afirmaciones de residentes-UI sin respaldo.\nCÓDIGO (zona censo+ficha):\n```\nlet PIRCACHE={key:null,mine:null,c:null,tot:0,con:0};\nfunction piramide(mine){\n  const c=new Map();let con=0;let tot=0;\n  for(const r of mine){\n    const k=r.grupo_edad+\"|\"+r.sexo;\n    const w=Number(r.peso)>0?Number(r.peso):1;\n    c.set(k,(c.get(k)||0)+w);\n    tot+=w;\n    if(r.regimen_salud===\"contributivo\")con++;\n  }\n  return {c,tot,con,n:mine.length};\n}\nfunction render(){\n  const nac=SEL.size===0;\n  const codes=nac?Object.keys(MARG):[...SEL];\n  const d=nac?natMarg():margDe(codes);\n  const KEY=nac?\"CO\":codes.slice().sort().join(\",\");\n  if(PIRCACHE.key!==KEY){\n    const m0=nac?RES:RES.filter(r=>SEL.has(r.dpto));\n    PIRCACHE={key:KEY,mine:m0,...piramide(m0)};\n  }\n  const mine=PIRCACHE.mine;\n  const dos=nac?{}:(DOS[cur]||{});\n  /* censo */\n  let h=\"\";\n  const rtotal=Object.values(d.edad||{}).reduce((a,b)=>a+b,0);\n  if(d.edad&&d.sexo&&rtotal>0){\n    h+=`<h3>PIRÁMIDE — real (línea) vs sintética (barra)</h3><div class=\"pyr\">`;\n    const tot=PIRCACHE.tot||1;\n    const chico=(PIRCACHE.n||0)<200;\n    const ags=[];\n    if(chico){\n      for(let i=0;i<AGEG.length;i+=2){\n        const a=AGEG[i],b=AGEG[i+1];\n        const na=a.match(/\\d+/g)||[],nb=b?(b.match(/\\d+/g)||[]):null;\n        ags.push({lab:b?(na[0]+\"-\"+nb[nb.length-1]+(/\\+$/.test(b)?\"+\":\"\")):a,gs:b?[a,b]:[a]});\n      }\n    } else for(const g of AGEG) ags.push({lab:g,gs:[g]});\n    const F=chico?3:6;\n    const ex=!!(d.edad_sexo&&d.edad_sexo.hombre&&d.edad_sexo.mujer);\n    for(const b of [...ags].reverse()){\n      let wH=0,wM=0,rh=0,rm=0;\n      for(const g of b.gs){\n        wH+=PIRCACHE.c.get(g+\"|hombre\")||0;\n        wM+=PIRCACHE.c.get(g+\"|mujer\")||0;\n        if(ex){rh+=(d.edad_sexo.hombre[g]||0)/rtotal*100;rm+=(d.edad_sexo.mujer[g]||0)/rtotal*100;}\n        else{rh+=(d.edad[g]||0)/rtotal*50;rm+=(d.edad[g]||0)/rtotal*50;}\n      }\n      const sh=wH/tot*100,sm=wM/tot*100;\n      h+=`<div class=\"bar l\"><i style=\"width:${sh*F}%\"></i><s style=\"right:${rh*F}%\"></s></div>\n          <div class=\"lab\">${b.lab}</div>\n          <div class=\"bar r\"><i style=\"width:${sm*F}%\"></i><s style=\"left:${rm*F}%\"></s></div>`;\n    }\n    h+=`</div><div class=\"leyenda\"><span><i style=\"background:var(--hom)\"></i>hombres</span>\n        <span><i style=\"background:var(--muj)\"></i>mujeres</span>\n        <span><i style=\"background:var(--text);width:2px\"></i>censo real</span></div>\n        <div class=\"fuente\">muestra sintética: n=${PIRCACHE.n||0} residentes (pesos → ${Math.round(PIRCACHE.tot||0).toLocaleString(\"es-CO\")} hab.)${(PIRCACHE.n||0)<200?\" · bandas de 10 años (muestra pequeña)\":\"\"}</div>`;\n  } else {\n    h+=`<h3>PIRÁMIDE</h3><div class=\"fuente\">⚠ sexo/edad provisionales — a la espera del\n        archivo de proyecciones DANE (el resto de bloques ya es real)</div>`;\n  }\n  const sal=d.salud_pct;\n  if(sal){\n    const got=PIRCACHE.con/(mine.length||1)*100;\n    h+=`<h3>RÉGIMEN DE SALUD</h3>\n    <div class=\"hbar\"><div class=\"t\"><span>contributivo real</span><b>${sal.contributivo}%</b></div>\n      <div class=\"g\"><i style=\"width:${sal.contributivo}%\"></i></div></div>\n    <div class=\"hbar\"><div class=\"t\"><span>contributivo sintético</span><b>${got.toFixed(0)}%</b></div>\n      <div class=\"g\"><i style=\"width:${got}%;background:var(--ok)\"></i></div></div>\n    <div class=\"fuente\">${esc(d.fuentes?.salud||\"\")}</div>`;\n  }\n  const pib=d.pib_sectores_pct;\n  if(pib){\n    h+=`<h3>DE QUÉ VIVE (PIB por sector)</h3>`;\n    const maxP=Math.max(...Object.values(pib))||1;\n    for(const [s,p] of Object.entries(pib))\n      h+=`<div class=\"hbar\"><div class=\"t\"><span>${esc(s.split(';')[0].trim())}</span><b>${p}%</b></div>\n          <div class=\"g\"><i style=\"width:${(p/maxP*100).toFixed(1)}%;background:var(--work)\"></i></div></div>`;\n    h+=`<div class=\"fuente\">${esc(d.fuentes?.pib||\"\")}</div>`;\n  }\n  $(\"#censobody\").innerHTML=h;\n  /* ficha */\n  if(nac){\n    $(\"#dossier\").innerHTML=`<b>Colombia entera en la mesa.</b><br><br>\n      ${(d.poblacion/1e6).toFixed(1)} millones de personas proyectadas por el DANE\n      para 2026, 33 territorios, 6.600 residentes sintéticos. En el panel nacional\n      cada departamento entra a la muestra según su población REAL: pesa más un\n      bogotano que un vaupense, como en el país de verdad.<br><br>\n      <span class=\"tag\" style=\"border-color:var(--ok)\">muestreo proporcional DANE</span>\n      <span class=\"tag\" style=\"border-color:var(--ok)\">33 voces regionales</span>`;\n  } else\n  $(\"#dossier\").innerHTML= dos.vida_cotidiana?\n    `<b>${esc(dos.apodo_gentilicio||d.nombre)}</b> · ${esc(dos.como_hablan||\"\")}<br><br>\n     ${esc(dos.vida_cotidiana)}<br><br><i style=\"color:var(--faint)\">${esc(dos.economia_vivida||\"\")}</i><br><br>\n     ${(dos.orgullos||[]).map(o=>`<span class=\"tag\" style=\"border-color:var(--ok)\">${esc(o)}</span>`).join(\"\")}\n     ${(dos.dolores||[]).map(o=>`<span class=\"tag\" style=\"border-color:var(--fail)\">${esc(o)}</span>`).join(\"\")}`\n    :`<i style=\"color:var(--faint)\">dossier en camino — lo está escribiendo el enjambre</i>`;\n  const mu=$(\"#muestra\");mu.innerHTML=\"\";\n  mine.slice(0,10).forEach(r=>{\n    const div=document.createElement(\"div\");div.className=\"res\";\n    RESBY[r.id]=r;div.dataset.rid=r.id;div.title=\"ver ficha DANE\";\n    const cv=document.createElement(\"canvas\");re\n```"}]}