{"task": "Implementación Jev v2: parches quirúrgicos desde los diseños aprobados.", "tasks": [{"id": "impl_autofix", "thinking": "high", "filename": "impl_autofix.json", "prompt": "Devuelve SOLO JSON crudo: {\"parches\":[{\"buscar\":\"...\",\"reemplazar\":\"...\"}]}. REGLAS DURAS: buscar = subcadena EXACTA y ÚNICA del código actual (cópiala literal); reemplazar = versión final completa; máx 6 parches quirúrgicos; NO reformatees nada fuera del cambio; conserva estilo y español. Integra las MEJORAS APROBADAS (su campo codigo es la referencia — adáptalo al archivo real).\n\nARCHIVO: scripts/autofix.py — integra la compuerta semántica Jev: jev_gate_parche() + su llamada en apply() ENTRE la compuerta de sintaxis y aplicados.append (si Jev rechaza → revertir y anotar motivo), + comando CLI 'gate' de auditoría seca. Usa OPENROUTER_API_KEY de os.environ y raíz .env vía dotenv si hace falta.\n\nMEJORAS APROBADAS:\n[{\"nombre\": \"jev_gate_parche(): compuerta semántica Jev completa (questions RECORD tipadas, fail-closed, bitácora)\", \"que\": \"Añade os, urllib.request y ThreadPoolExecutor a los imports; agrega las constantes JEV_URL/JEV_MODEL/JEV_TIMEOUT/RIESGO_MAX/JEV_APAGADA/JEV_LOG justo debajo de DEFAULT_FILE; y define jev_gate_parche(hallazgo, buscar, reemplazar) completo. Envía a POST /api/alpha/decisions un questions RECORD con 3 claves tipadas (resuelve=noul, riesgo=score, efecto_colateral=noul), lee answers.<k>.noul y answers.<k>.score, y aprueba SOLO si el noul de 'resuelve' contiene 'VEREDICTO: APROBAR' Y el score de riesgo <= RIESGO_MAX. Sin OPENROUTER_API_KEY, error de red/timeout/HTTP o score ilegible => aprobar=False. Usa el json ya importado (no lo reimportes) y escribe cada veredicto en orchestrator/jev_gate.jsonl.\", \"codigo\": \"# ── 1) Bloque de imports final de autofix.py (líneas 8-16) ─────────────────\\nimport json\\nimport os\\nimport re\\nimport subprocess\\nimport sys\\nimport urllib.request\\nfrom concurrent.futures import ThreadPoolExecutor\\nfrom pathlib import Path\\n\\n# ── 2) Pega esto justo DEBAJO de DEFAULT_FILE = 'dashboard/sim/index.html' ──\\nJEV_URL = 'https://openrouter.ai/api/v1/alpha/decisions'\\nJEV_MODEL = 'typesafe/jev-1.13'\\nJEV_TIMEOUT = 90          # segundos por llamada\\nRIESGO_MAX = 40           # score > RIESGO_MAX => RECHAZA. Calibra con: autofix.py gate <run_dir>\\nJEV_APAGADA = os.environ.get('JEV_GATE', '1') == '0'   # JEV_GATE=0 => compuerta off (emergencia)\\nJEV_LOG = ROOT / 'orchestrator' / 'jev_gate.jsonl'\\n\\n\\ndef jev_gate_parche(hallazgo: dict, buscar: str, reemplazar: str) -> dict:\\n    '''Compuerta semántica: ¿este parche RESUELVE el hallazgo SIN romper la lógica?\\n    (compilar != ser correcto: flags de stop invertidos, guardas desactivadas,\\n    comparaciones giradas, estado mutado, orden de efectos, excepciones tragadas).\\n    FAIL-CLOSED: sin API key, error de red o respuesta ilegible => aprobar=False.\\n    Devuelve {'aprobar','riesgo','resuelve','efecto_colateral','motivo','costo_usd'}.'''\\n    res = {'aprobar': False, 'riesgo': 100, 'resuelve': '',\\n           'efecto_colateral': '', 'motivo': '', 'costo_usd': 0.0}\\n\\n    if JEV_APAGADA:\\n        res.update(aprobar=True, riesgo=0, motivo='compuerta desactivada (JEV_GATE=0)')\\n        return res\\n\\n    key = os.environ.get('OPENROUTER_API_KEY', '').strip()\\n    if not key:\\n        res['motivo'] = 'sin OPENROUTER_API_KEY — fail-closed'\\n        return res\\n\\n    payload = {\\n        'model': JEV_MODEL,\\n        'questions': {\\n            'resuelve': {\\n                'type': 'noul',\\n                'instructions': (\\n                    'Revisas parches de código. Decide si REEMPLAZAR resuelve EXACTAMENTE el '\\n                    'HALLAZGO sin alterar nada más. Juzga SEMÁNTICA, no sintaxis: flags/stop '\\n                    'invertidos, guardas desactivadas, condiciones giradas (>= vs >), estado '\\n                    'mutado de más, orden de efectos, off-by-one, excepciones tragadas.\\\\n'\\n                    f'HALLAZGO: {json.dumps(hallazgo, ensure_ascii=False)}\\\\n'\\n                    f'BUSCAR (código actual, literal):\\\\n{buscar}\\\\n'\\n                    f'REEMPLAZAR (parche propuesto, literal):\\\\n{reemplazar}'\\n                ),\\n                'criteria': (\\n                    'Justifica en <=80 palabras y TERMINA con una línea exacta: '\\n                    \\\"'VEREDICTO: APROBAR' si resuelve el hallazgo sin efecto colateral, o \\\"\\n                    \\\"'VEREDICTO: RECHAZAR' en cualquier otro caso (incluida la duda).\\\"\\n                ),\\n            },\\n            'riesgo': {\\n                'type': 'score',\\n                'instructions': (\\n                    'Riesgo de romper comportamiento existente al aplicar el parche en '\\n                    'producción. 0 = trivial y demostrablemente seguro; 100 = cambia semántica '\\n                    'fuera del hallazgo.'\\n                ),\\n                'criteria': ('Entero 0-100. Sube por cada rama/llamador afectado, flag o guarda '\\n                             'tocada o condición de borde cambiada.'),\\n            },\\n            'efecto_colateral': {\\n                'type': 'noul',\\n                'instructions': ('¿Qué OTRAS rutas, llamadores, flags o contratos cambian de '\\n                                 'comportamiento si se aplica REEMPLAZAR? Sé quirúrgico y breve.'),\\n                'criteria': 'Si no hay ninguno, responde exactamente: NINGUNO',\\n            },\\n        },\\n    }\\n\\n    req = urllib.request.Request(\\n        JEV_URL,\\n        data=json.dumps(payload, ensure_ascii=False).encode('utf-8'),\\n        headers={'Authorization': f'Bearer {key}', 'Content-Type': 'application/json'},\\n        method='POST',\\n    )\\n    try:\\n        with urllib.request.urlopen(req, timeout=JEV_TIMEOUT) as r:\\n            data = json.loads(r.read().decode('utf-8'))\\n    except Exception as e:                      # red, 4xx/5xx, timeout, JSON roto\\n        res['motivo'] = f'Jev inaccesible ({type(e).__name__}: {e}) — fail-closed'\\n        return res\\n\\n    ans = data.get('answers') or (data.get('data') or {}).get('answers') or {}\\n    noul_res = str((ans.get('resuelve') or {}).get('noul', '') or '')\\n    noul_col = str((ans.get('efecto_colateral') or {}).get('noul', '') or '')\\n    score_raw = (ans.get('riesgo') or {}).get('score', None)\\n    res['resuelve'] = noul_res.strip()\\n    res['efecto_colateral'] = noul_col.strip()\\n    try:\\n        riesgo = int(round(float(score_raw)))   # tipado: el score debe ser numérico\\n    except (TypeError, ValueError):\\n        res['motivo'] = 'respuesta Jev ilegible (sin score numérico) — fail-closed'\\n        return res\\n\\n    aprobar = ('VEREDICTO: APROBAR' in noul_res.upper()) and riesgo <= RIESGO_MAX\\n    res.update(aprobar=aprobar, riesgo=riesgo,\\n               motivo='' if aprobar else (f'riesgo {riesgo} > {RIESGO_MAX}'\\n                                          if riesgo > RIESGO_MAX else 'veredicto no-APROBAR'),\\n               costo_usd=float((data.get('usage') or {}).get('cost', 0.0) or 0.0))\\n    try:                                        # bitácora auditable de cada veredicto\\n        with JEV_LOG.open('a') as fh:\\n            fh.write(json.dumps({'archivo': hallazgo.get('archivo', ''), 'aprobar': aprobar,\\n                                 'riesgo': riesgo, 'motivo': res['motivo'],\\n                                 'resuelve': res['resuelve'][:400],\\n                                 'efecto_colateral': res['efecto_colateral'][:200],\\n                                 'buscar': buscar[:160], 'reemplazar': reemplazar[:160]},\\n                                ensure_ascii=False) + '\\\\n')\\n    except Exception:\\n        pass\\n    return res\\n\", \"impacto\": \"Convierte 'compila' en 'es correcto': el flag de stop invertido que pasó antes ahora se rechaza antes de tocar disco.\"}, {\"nombre\": \"_hallazgo_de() y jev_gate_lote(): contexto real del hallazgo + evaluación en paralelo\", \"que\": \"Dos helpers nuevos. _hallazgo_de() reconstruye el hallazgo original (fx_<stem>_<i>.json -> artifacts/<stem>.json, índice i, filtrando gravedad alta) para que Jev juzgue contra el problema real y no contra un string vacío; si falla, degrada a {'archivo','tarea'} sin romper. jev_gate_lote() evalúa N parches con ThreadPoolExecutor (máx 4 hilos) para que 3 parches no serialicen 3 timeouts de 90 s = 270 s por archivo.\", \"codigo\": \"def _hallazgo_de(run_dir: Path, f: Path, archivo: str) -> dict:\\n    '''Recupera el hallazgo original que originó fx_<stem>_<i>.json, para dar contexto real a Jev.'''\\n    stem, _, idx = f.stem.replace('fx_', '', 1).rpartition('_')\\n    try:\\n        d = json.loads(clean((run_dir / 'artifacts' / f'{stem}.json').read_text()))\\n        altas = [h for h in d.get('hallazgos', []) if h.get('gravedad') == 'alta']\\n        h = dict(altas[int(idx)])\\n    except Exception:\\n        h = {}\\n    h.setdefault('archivo', archivo)\\n    h.setdefault('tarea', stem)\\n    return h\\n\\n\\ndef jev_gate_lote(hallazgo: dict, pares: list) -> list:\\n    '''Juzga TODOS los parches de un archivo en paralelo (3x90 s en serie bloquean; en hilo no).\\n    pares = [(buscar, reemplazar), ...] -> lista de veredictos en el MISMO orden.'''\\n    if not pares:\\n        return []\\n    with ThreadPoolExecutor(max_workers=min(4, len(pares))) as ex:\\n        return list(ex.map(lambda br: jev_gate_parche(hallazgo, br[0], br[1]), pares))\\n\", \"impacto\": \"Jev ve el hallazgo literal en vez de un dict vacío, y el costo de latencia deja de crecer lineal con el número de parches.\"}, {\"nombre\": \"apply() con compuerta 3 (semántica) integrada entre sintaxis y commit\", \"que\": \"Reemplaza apply() completo. Orden de compuertas: (1) match exacto y único en memoria; (2) escritura + check de sintaxis, revirtiendo si falla; (3) NUEVA: jev_gate_lote() sobre los pares realmente aplicados; si cualquier veredicto trae aprobar=False se restaura el original y el archivo se rechaza con detalle (índice del parche, riesgo y motivo). Sin match único ya no se llega a pagar la llamada Jev, y el revert es siempre el texto original íntegro.\", \"codigo\": \"def apply(run_dir: Path):\\n    aplicados, rechazados = [], []\\n    for f in sorted(run_dir.glob('artifacts/fx_*.json')):\\n        try:\\n            parches = json.loads(clean(f.read_text())).get('parches', [])\\n        except Exception:\\n            rechazados.append((f.stem, 'json inválido')); continue\\n        stem = f.stem.replace('fx_', '').rsplit('_', 1)[0]\\n        rel = archivo_de(stem)\\n        archivo = ROOT / rel\\n        original = archivo.read_text()\\n        texto, ok, pares = original, True, []\\n        for p in parches:\\n            b, r = p.get('buscar', ''), p.get('reemplazar', '')\\n            if not b or texto.count(b) != 1:      # compuerta 1: match exacto y único\\n                ok = False; break\\n            texto = texto.replace(b, r)\\n            pares.append((b, r))\\n        if not ok or texto == original:\\n            rechazados.append((f.stem, 'sin match único o vacío')); continue\\n        archivo.write_text(texto)\\n        if not check(archivo):                     # compuerta 2: sintaxis\\n            archivo.write_text(original)\\n            rechazados.append((f.stem, 'rompía sintaxis — revertido')); continue\\n        # compuerta 3: SEMÁNTICA (Jev) — un parche puede compilar y aun envenenar la lógica\\n        veredictos = jev_gate_lote(_hallazgo_de(run_dir, f, rel), pares)\\n        malos = [(i, v) for i, v in enumerate(veredictos) if not v['aprobar']]\\n        if malos:\\n            archivo.write_text(original)\\n            detalle = '; '.join(f\\\"parche[{i}] riesgo={v['riesgo']} ({v['motivo']})\\\" for i, v in malos)\\n            rechazados.append((f.stem, f'Jev RECHAZA — revertido: {detalle}'))\\n            continue\\n        aplicados.append(f.stem)\\n    print(f'✅ aplicados: {aplicados}')\\n    print(f'⛔ rechazados: {rechazados or \\\"ninguno\\\"}')\\n\", \"impacto\": \"Ningún parche llega a producción sin que Jev certifique que resuelve el hallazgo y no mueve la semántica; los rechazos quedan explicados, no en silencio.\"}, {\"nombre\": \"CLI 'gate <run_dir>': auditoría seca + calibración de RIESGO_MAX, y dispatch de 3 comandos\", \"que\": \"Comando nuevo que juzga los parches de un run SIN tocar archivos (dry-run): omite los que no tienen match único, imprime APRUEBA/RECHAZA por parche con riesgo, efecto colateral y motivo, y cierra con el conteo y el costo estimado (~$0.00003/llamada) para calibrar RIESGO_MAX antes de aplicar. El __main__ pasa de ternario a dispatch por dict con los tres comandos.\", \"codigo\": \"def gate(run_dir: Path):\\n    '''Auditoría SECA (dry-run): juzga los parches pendientes sin tocar archivos.\\n    Sirve para calibrar RIESGO_MAX y ver el costo antes de aplicar.'''\\n    ok = no = 0\\n    for f in sorted(run_dir.glob('artifacts/fx_*.json')):\\n        try:\\n            parches = json.loads(clean(f.read_text())).get('parches', [])\\n        except Exception:\\n            print(f'{f.stem}: json inválido'); continue\\n        stem = f.stem.replace('fx_', '').rsplit('_', 1)[0]\\n        rel = archivo_de(stem)\\n        codigo = (ROOT / rel).read_text()\\n        hallazgo = _hallazgo_de(run_dir, f, rel)\\n        usables = [(p.get('buscar', ''), p.get('reemplazar', '')) for p in parches\\n                   if p.get('buscar') and codigo.count(p['buscar']) == 1]\\n        if len(usables) != len(parches):\\n            print(f'{f.stem}: {len(parches) - len(usables)} parche(s) sin match único — omitidos')\\n        for i, v in enumerate(jev_gate_lote(hallazgo, usables)):\\n            print(f\\\"{'APRUEBA' if v['aprobar'] else 'RECHAZA'} {f.stem}[{i}] \\\"\\n                  f\\\"riesgo={v['riesgo']:>3} colateral={v['efecto_colateral'][:60]!r} \\\"\\n                  f\\\"motivo={v['motivo'] or '-'}\\\")\\n            ok += 1 if v['aprobar'] else 0\\n            no += 0 if v['aprobar'] else 1\\n    print(f'\\\\n{run_dir.name}: {ok} aprobables, {no} rechazables '\\n          f'(costo ~${0.00003 * (ok + no):.6f})')\\n\\n\\nif __name__ == '__main__':\\n    cmd, run = sys.argv[1], Path(sys.argv[2])\\n    run = run if run.is_absolute() else ROOT / run\\n    {'plan': plan, 'apply': apply, 'gate': gate}[cmd](run)\\n\", \"impacto\": \"Permite ver qué rechazaría Jev y a qué costo ANTES de escribir un solo byte, y ajustar RIESGO_MAX con datos en vez de a ciegas.\"}]\n\nCÓDIGO ACTUAL COMPLETO:\n```\n#!/usr/bin/env python3\n\"\"\"Ciclo autónomo de fixes: hallazgos → agentes escriben PARCHES → se aplican\ncon compuertas (match exacto + chequeo de sintaxis). Claude aprueba el lote.\n\nUso:\n  python scripts/autofix.py plan <run_dir_hallazgos>   # emite orchestrator/plans/fixes.json\n  python scripts/autofix.py apply <run_dir_parches>    # aplica con compuertas\n\"\"\"\nimport json\nimport re\nimport subprocess\nimport sys\nfrom pathlib import Path\n\nROOT = Path(__file__).resolve().parent.parent\nARCHIVO_POR_TAREA = {  # de qué archivo habla cada tarea de hallazgos\n    \"r2_api\": \"api/opina.js\",\n    \"r5_showtime\": \"simcolombia/fable/showtime.py\",\n    \"r5_effswarm\": \"orchestrator/swarm.py\",\n    \"r5_effsim\": \"dashboard/sim/index.html\",\n    \"r5_visor2\": \"dashboard/index.html\",\n    \"r5_gen2\": \"simcolombia/pipeline/generate_population.py\",\n    # nota: los parches se validan por match-único contra el archivo destino,\n    # así que un mapeo errado se rechaza solo — pero mejor acertar:\n}\nSALTAR = {\"r5_evento\"}  # hallazgos estructurales (guion) — los arregla Claude a mano\ndef archivo_de(stem):\n    if stem in ARCHIVO_POR_TAREA:\n        return ARCHIVO_POR_TAREA[stem]\n    return \"api/opina.js\" if \"api\" in stem else DEFAULT_FILE\nDEFAULT_FILE = \"dashboard/sim/index.html\"\n\n\ndef clean(t):\n    return re.sub(r\"^```(?:json)?|```$\", \"\", t.strip(), flags=re.M).strip()\n\n\ndef plan(run_dir: Path):\n    tasks = []\n    for f in sorted(run_dir.glob(\"artifacts/*.json\")):\n        try:\n            d = json.loads(clean(f.read_text()))\n        except Exception:\n            continue\n        if f.stem in SALTAR:\n            continue\n        archivo = archivo_de(f.stem)\n        code = (ROOT / archivo).read_text()\n        altas = [h for h in d.get(\"hallazgos\", []) if h.get(\"gravedad\") == \"alta\"]\n        for i, h in enumerate(altas[:3]):\n            tasks.append({\n                \"id\": f\"fx_{f.stem}_{i}\", \"thinking\": \"medium\",\n                \"filename\": f\"fx_{f.stem}_{i}.json\",\n                \"prompt\": (\n                    'Devuelve SOLO JSON crudo: {\"parches\":[{\"buscar\":\"...\",\"reemplazar\":\"...\"}]} '\n                    \"— máximo 3 parches quirúrgicos que arreglen SOLO este hallazgo. \"\n                    \"REGLAS DURAS: 'buscar' debe ser una subcadena EXACTA y ÚNICA del código \"\n                    \"(cópiala literal, con sus espacios y saltos); 'reemplazar' la versión \"\n                    \"corregida completa de ese fragmento; NO reformatees nada más; NO toques \"\n                    \"otras funciones; conserva el estilo. Si el hallazgo no es arreglable con \"\n                    'parches seguros, devuelve {\"parches\":[]}.\\n\\n'\n                    f\"HALLAZGO en {archivo}: [{h.get('donde','?')}] {h.get('problema','')} \"\n                    f\"FIX SUGERIDO: {h.get('fix','')}\\n\\nCÓDIGO COMPLETO:\\n```\\n{code[:60000]}\\n```\"\n                ),\n            })\n    out = ROOT / \"orchestrator\" / \"plans\" / \"fixes.json\"\n    json.dump({\"task\": \"Parches quirúrgicos para los hallazgos de gravedad alta.\",\n               \"tasks\": tasks}, out.open(\"w\"), ensure_ascii=False)\n    print(f\"{len(tasks)} tareas de fix → {out}\")\n\n\ndef check(path: Path) -> bool:\n    if path.suffix == \".js\":\n        return subprocess.run([\"node\", \"--check\", str(path)],\n                              capture_output=True).returncode == 0\n    if path.suffix == \".py\":\n        return subprocess.run([sys.executable, \"-m\", \"py_compile\", str(path)],\n                              capture_output=True).returncode == 0\n    if path.suffix == \".html\":\n        js = re.search(r\"<script>(.*)</script>\", path.read_text(), re.S)\n        p = Path(\"/tmp/_chk.js\"); p.write_text(js.group(1) if js else \"\")\n        return subprocess.run([\"node\", \"--check\", str(p)],\n                              capture_output=True).returncode == 0\n    return True\n\n\ndef apply(run_dir: Path):\n    aplicados, rechazados = [], []\n    for f in sorted(run_dir.glob(\"artifacts/fx_*.json\")):\n        try:\n            parches = json.loads(clean(f.read_text())).get(\"parches\", [])\n        except Exception:\n            rechazados.append((f.stem, \"json inválido\")); continue\n        stem = f.stem.replace(\"fx_\", \"\").rsplit(\"_\", 1)[0]\n        archivo = ROOT / archivo_de(stem)\n        original = archivo.read_text()\n        texto = original\n        ok = True\n        for p in parches:\n            b, r = p.get(\"buscar\", \"\"), p.get(\"reemplazar\", \"\")\n            if not b or texto.count(b) != 1:      # compuerta 1: match exacto y único\n                ok = False; break\n            texto = texto.replace(b, r)\n        if not ok or texto == original:\n            rechazados.append((f.stem, \"sin match único o vacío\")); continue\n        archivo.write_text(texto)\n        if not check(archivo):                     # compuerta 2: sintaxis\n            archivo.write_text(original)\n            rechazados.append((f.stem, \"rompía sintaxis — revertido\")); continue\n        aplicados.append(f.stem)\n    print(f\"✅ aplicados: {aplicados}\")\n    print(f\"⛔ rechazados: {rechazados or 'ninguno'}\")\n\n\nif __name__ == \"__main__\":\n    cmd, run = sys.argv[1], Path(sys.argv[2])\n    run = run if run.is_absolute() else ROOT / run\n    plan(run) if cmd == \"plan\" else apply(run)\n\n```"}, {"id": "impl_swarm", "thinking": "high", "filename": "impl_swarm.json", "prompt": "Devuelve SOLO JSON crudo: {\"parches\":[{\"buscar\":\"...\",\"reemplazar\":\"...\"}]}. REGLAS DURAS: buscar = subcadena EXACTA y ÚNICA del código actual (cópiala literal); reemplazar = versión final completa; máx 6 parches quirúrgicos; NO reformatees nada fuera del cambio; conserva estilo y español. Integra las MEJORAS APROBADAS (su campo codigo es la referencia — adáptalo al archivo real).\n\nARCHIVO: orchestrator/swarm.py — integra: (1) JEV_MAX_CALLS por corrida (env, default 60; al superarlo → gate heurístico local); (2) diagnóstico sesgo_estereotipo agregado a las questions de jev_review y a la escalera (si ese diag <0.5 el fix_prompt lo dice explícito); (3) escalar_a_modelo_pro EFECTIVA: cambia model a deepseek-v4-pro en el siguiente intento y lo loguea. NO metas el batching todavía (va en otra pasada).\n\nMEJORAS APROBADAS:\n[{\"nombre\": \"Batching de revisiones: N tareas en 1 llamada a Jev\", \"que\": \"Añade jev_review_batch(items) que revisa un lote [(id, prompt, output)] en UNA sola llamada: el state numera cada tarea ([i] TAREA / [i] OUTPUT) y las questions se generan por índice (cumple_i, calidad_i, accion_i, <diag>_i), devolviendo {id: verdict} con el MISMO esquema que jev_review. Descarta outputs <80 chars ANTES de gastar la llamada y respeta el techo de llamadas.\", \"codigo\": \"async def jev_review_batch(items):\\n    \\\"\\\"\\\"Revisa N tareas en UNA llamada (state numerado, questions por indice).\\n    items: [(id, task_prompt, output)]. Devuelve {id: verdict} con el MISMO\\n    esquema que jev_review. Coste ~1 revision -> ideal para dossiers/microdatos.\\\"\\\"\\\"\\n    items = [(tid, tp, out) for tid, tp, out in items if out and len(out) >= 80]\\n    if not items or not jev_budget_ok():\\n        return {}\\n    try:\\n        import httpx\\n        parts, q = [], {}\\n        for i, (tid, tp, out) in enumerate(items):\\n            parts.append(f\\\"[{i}] TAREA: {tp[:1500]}\\\\n[{i}] OUTPUT: {out[:3000]}\\\")\\n            q[f\\\"cumple_{i}\\\"] = {\\\"type\\\": \\\"noul\\\",\\n                \\\"instructions\\\": f\\\"[{i}] El output entregado cumple la tarea encargada\\\"}\\n            q[f\\\"calidad_{i}\\\"] = {\\\"type\\\": \\\"score\\\",\\n                \\\"instructions\\\": f\\\"[{i}] Calidad del output entregado\\\",\\n                \\\"criteria\\\": [\\\"inservible\\\", \\\"pobre\\\", \\\"aceptable\\\", \\\"bueno\\\", \\\"excelente\\\"]}\\n            q[f\\\"accion_{i}\\\"] = {\\\"type\\\": \\\"choice\\\",\\n                \\\"instructions\\\": f\\\"[{i}] La mejor accion sobre este output\\\",\\n                \\\"criteria\\\": {\\n                    \\\"aprobar\\\": \\\"el output cumple, entregarlo tal cual\\\",\\n                    \\\"fix_con_feedback\\\": \\\"errores menores, corregir con notas del inspector\\\",\\n                    \\\"reintentar_pensando_mas\\\": \\\"fallo de razonamiento, reintentar con mas pensamiento\\\",\\n                    \\\"escalar_a_modelo_pro\\\": \\\"demasiado dificil para el modelo rapido, escalar\\\"}}\\n            for d in DIAG_KEYS:\\n                q[f\\\"{d}_{i}\\\"] = {\\\"type\\\": \\\"noul\\\",\\n                    \\\"instructions\\\": f\\\"[{i}] {DIAG_INSTR[d]}\\\"}\\n        async with httpx.AsyncClient(timeout=60) as c:\\n            r = await c.post(\\n                \\\"https://openrouter.ai/api/alpha/decisions\\\",\\n                headers={\\\"Authorization\\\": f\\\"Bearer {os.environ['OPENROUTER_API_KEY']}\\\"},\\n                json={\\\"model\\\": JEV_MODEL, \\\"state\\\": \\\"\\\\n\\\\n\\\".join(parts), \\\"questions\\\": q})\\n            d = r.json()\\n            budget.spent[\\\"openrouter\\\"] += float(d.get(\\\"usage\\\", {}).get(\\\"cost\\\") or 0)\\n            a = d[\\\"answers\\\"]\\n            res = {}\\n            for i, (tid, _, _) in enumerate(items):\\n                diag = {k: float(a[f\\\"{k}_{i}\\\"][\\\"noul\\\"]) for k in DIAG_KEYS}\\n                res[tid] = {\\\"p\\\": float(a[f\\\"cumple_{i}\\\"][\\\"noul\\\"]),\\n                            \\\"quality\\\": float(a[f\\\"calidad_{i}\\\"][\\\"score\\\"]),\\n                            \\\"action\\\": a[f\\\"accion_{i}\\\"][\\\"choice\\\"],\\n                            \\\"diag\\\": diag,\\n                            \\\"fallas\\\": [k for k, v in diag.items() if v < 0.5]}\\n            return res\\n    except Exception:\\n        return {}\", \"impacto\": \"Baja el coste de revision ~Nx en lotes de microdatos/dossiers electorales sin cambiar la semantica del veredicto.\"}, {\"nombre\": \"Techo de llamadas a Jev por corrida (JEV_MAX_CALLS)\", \"que\": \"Añade el contador JEV_CALLS y el guard jev_budget_ok(n): si se supera JEV_MAX_CALLS (env, def. 400) la revision devuelve None y el harness cae al gate heuristico local en vez de seguir pagando a Jev. Se inserta como primera linea de jev_review y de jev_review_batch.\", \"codigo\": \"JEV_MAX_CALLS = int(os.environ.get(\\\"JEV_MAX_CALLS\\\", \\\"400\\\"))\\nJEV_CALLS = {\\\"n\\\": 0}\\n\\ndef jev_budget_ok(n=1):\\n    \\\"\\\"\\\"Techo de llamadas a Jev por corrida. Al agotarse devuelve False y la\\n    revision retorna None -> el harness cae al gate heuristico local (ship-first).\\\"\\\"\\\"\\n    if JEV_CALLS[\\\"n\\\"] + n > JEV_MAX_CALLS:\\n        return False\\n    JEV_CALLS[\\\"n\\\"] += n\\n    return True\\n\\n# Primera linea de jev_review Y de jev_review_batch, ANTES del try:\\nasync def jev_review(task_prompt: str, output: str):\\n    if not jev_budget_ok():\\n        return None\\n    try:\\n        ...\", \"impacto\": \"Pone un tope duro al gasto de inspeccion por corrida; nunca bloquea el ship.\"}, {\"nombre\": \"Diagnostico sesgo_estereotipo + FALLA_NOTA accionable\", \"que\": \"Extiende DIAG_INSTR/DIAG_KEYS con 'sesgo_estereotipo' (territorio, instituciones y actores tratados con datos/matices locales, sin generalizaciones), deriva DIAG_QUESTIONS para inyectar en jev_review y jev_review_batch, y añade FALLA_NOTA + fallas_a_nota() que traduce cada falla en una instruccion concreta para el prompt de reintento (antes era un texto generico 'relee las restricciones').\", \"codigo\": \"DIAG_INSTR = {\\n    \\\"completo\\\": \\\"el output esta completo, no le falta ninguna parte pedida\\\",\\n    \\\"restricciones\\\": \\\"respeta las restricciones explicitas (longitud exacta, formato, cantidad)\\\",\\n    \\\"sin_relleno\\\": \\\"va al grano, sin relleno ni preambulos\\\",\\n    \\\"sesgo_estereotipo\\\": (\\\"trata el territorio, sus instituciones y sus actores con datos \\\"\\n                          \\\"y matices locales; no usa estereotipos ni generalizaciones sobre la region\\\"),\\n}\\nDIAG_KEYS = tuple(DIAG_INSTR)\\nDIAG_QUESTIONS = {k: {\\\"type\\\": \\\"noul\\\", \\\"instructions\\\": DIAG_INSTR[k]} for k in DIAG_KEYS}\\n\\nFALLA_NOTA = {\\n    \\\"completo\\\": \\\"Entrega TODAS las partes pedidas; no omitas ninguna seccion ni dato.\\\",\\n    \\\"restricciones\\\": \\\"Relee las restricciones LITERALES (longitud exacta, formato, cantidad) y cumplelas al pie de la letra.\\\",\\n    \\\"sin_relleno\\\": \\\"Elimina preambulos y relleno; ve directo al contenido.\\\",\\n    \\\"sesgo_estereotipo\\\": \\\"Sustituye generalizaciones sobre el territorio por datos concretos, series y fuentes locales; usa el marco institucional real.\\\",\\n}\\n\\ndef fallas_a_nota(fallas):\\n    \\\"\\\"\\\"Falla -> instruccion concreta para el reintento (rechazo ACCIONABLE).\\\"\\\"\\\"\\n    if not fallas:\\n        return \\\" Relee las restricciones LITERALES de la tarea (longitud exacta, formato, cantidad) y cumplelas.\\\"\\n    return \\\" \\\" + \\\" \\\".join(FALLA_NOTA[k] for k in fallas)\\n\\n# En jev_review / jev_review_batch, el dict \\\"questions\\\" queda:\\n#   \\\"questions\\\": {\\n#       \\\"cumple\\\": {\\\"type\\\": \\\"noul\\\", \\\"instructions\\\": \\\"El output entregado cumple la tarea encargada\\\"},\\n#       \\\"calidad\\\": {\\\"type\\\": \\\"score\\\", \\\"instructions\\\": \\\"Calidad del output entregado\\\",\\n#                   \\\"criteria\\\": [\\\"inservible\\\", \\\"pobre\\\", \\\"aceptable\\\", \\\"bueno\\\", \\\"excelente\\\"]},\\n#       \\\"accion\\\": {\\\"type\\\": \\\"choice\\\", \\\"instructions\\\": \\\"La mejor accion sobre este output\\\",\\n#                  \\\"criteria\\\": {\\\"aprobar\\\": \\\"...\\\", \\\"fix_con_feedback\\\": \\\"...\\\",\\n#                               \\\"reintentar_pensando_mas\\\": \\\"...\\\", \\\"escalar_a_modelo_pro\\\": \\\"...\\\"}},\\n#       **DIAG_QUESTIONS,\\n#   }\\n# y el retorno:  diag = {k: float(a[k][\\\"noul\\\"]) for k in DIAG_KEYS}\\n#                fallas = [k for k, v in diag.items() if v < 0.5]\", \"impacto\": \"El rechazo deja de ser 'relee las restricciones' y pasa a ser una correccion dirigida, clave en contenido politico territorial.\"}, {\"nombre\": \"escalar_a_modelo_pro efectiva + trazable\", \"que\": \"Hoy la accion cambia model a un literal fijo y no se registra (se ignora en telemetria). Ahora PRO_MODEL es configurable por env, se cuenta en ESCALATIONS y se loguea el evento 'escalate'; si ya estamos en PRO_MODEL, escala el pensamiento en vez de repetir modelo. El prompt de reintento usa fallas_a_nota() en lugar del texto generico.\", \"codigo\": \"PRO_MODEL = os.environ.get(\\\"PRO_MODEL\\\", \\\"deepseek-v4-pro\\\")\\nESCALATIONS = {\\\"n\\\": 0}\\n\\n# Dentro del bucle de reintentos, donde hoy se aplica la accion de Jev:\\nif jv[\\\"action\\\"] == \\\"reintentar_pensando_mas\\\":\\n    think = THINK_UP[think]\\nelif jv[\\\"action\\\"] == \\\"escalar_a_modelo_pro\\\" and model != PRO_MODEL:\\n    ESCALATIONS[\\\"n\\\"] += 1\\n    self.log({\\\"event\\\": \\\"escalate\\\", \\\"id\\\": t[\\\"id\\\"], \\\"from\\\": model, \\\"to\\\": PRO_MODEL})\\n    model, think = PRO_MODEL, \\\"medium\\\"   # efectiva, persistente y trazable\\nelif jv[\\\"action\\\"] == \\\"escalar_a_modelo_pro\\\":\\n    think = THINK_UP[think]               # ya en pro -> sube pensamiento\\n\\nprint(f\\\"  \\\\U0001f527 {t['id']} rechazado por Jev (p={jv['p']:.2f}, \\\"\\n      f\\\"calidad={jv['quality']:.1f}) -> {jv['action']}\\\")\\nout = await llm(\\n    f\\\"Un inspector califico tu output {jv['quality']:.1f}/4 y lo rechazo.\\\"\\n    f\\\"{fallas_a_nota(jv['fallas'])}\\\\n\\\"\\n    f\\\"Output rechazado:\\\\n{out[:1500]}\\\\n\\\\n\\\"\\n    f\\\"Entrega la version corregida y completa de: {t['prompt']}\\\",\\n    model=model, system=WORKER_CONSTITUTION, thinking=think)\", \"impacto\": \"La escalada a pro deja de ignorarse: se aplica, persiste en el siguiente intento y queda en telemetria.\"}]\n\nZONAS RELEVANTES DEL CÓDIGO (jev_review + escalera):\n```\nasync def jev_review(task_prompt: str, output: str):\n    \"\"\"Jev como inspector Y enrutador: UNA llamada, tres decisiones tipadas.\n\n    - cumple (noul): ¿el output cumple la tarea? → gate\n    - calidad (score 0-4): telemetría de deriva de calidad\n    - accion (choice): SI NO cumple, qué hacer — esto es la escalera de\n      reintentos decidida por un juez calibrado, no por reglas ciegas.\n    Devuelve dict o None si Jev no responde (→ gate heurístico local).\n    ~$0.00003 por inspección; output tipado, sin parsing, sin loops de juez.\n    \"\"\"\n    try:\n        import httpx\n        async with httpx.AsyncClient(timeout=30) as c:\n            r = await c.post(\n                \"https://openrouter.ai/api/alpha/decisions\",\n                headers={\"Authorization\": f\"Bearer {os.environ['OPENROUTER_API_KEY']}\"},\n                json={\n                    \"model\": JEV_MODEL,\n                    \"state\": f\"Tarea encargada:\\n{task_prompt[:4000]}\\n\\n\"\n                             f\"Output entregado:\\n{output[:8000]}\",\n                    \"questions\": {\n                        \"cumple\": {\"type\": \"noul\",\n                            \"instructions\": \"El output entregado cumple la tarea encargada\"},\n                        \"calidad\": {\"type\": \"score\",\n                            \"instructions\": \"Calidad del output entregado\",\n                            \"criteria\": [\"inservible\", \"pobre\", \"aceptable\", \"bueno\", \"excelente\"]},\n                        \"accion\": {\"type\": \"choice\",\n                            \"instructions\": \"La mejor acción sobre este output\",\n                            \"criteria\": {\n                                \"aprobar\": \"el output cumple, entregarlo tal cual\",\n                                \"fix_con_feedback\": \"errores menores, corregir con notas del inspector\",\n                                \"reintentar_pensando_mas\": \"fallo de razonamiento, reintentar con más pensamiento\",\n                                \"escalar_a_modelo_pro\": \"demasiado difícil para el modelo rápido, escalar\",\n                            }},\n                        # diagnósticos: hacen el rechazo ACCIONABLE en el fix\n                        \"completo\": {\"type\": \"noul\",\n                            \"instructions\": \"El output está completo, no le falta ninguna parte pedida\"},\n                        \"restricciones\": {\"type\": \"noul\",\n                            \"instructions\": \"El output respeta las restricciones explícitas de la tarea (longitud, formato, cantidad)\"},\n                        \"sin_relleno\": {\"type\": \"noul\",\n                            \"instructions\": \"El output va al grano, sin relleno ni preámbulos\"},\n                    },\n                },\n            )\n            d = r.json()\n            budget.spent[\"openrouter\"] += float(d.get(\"usage\", {}).get(\"cost\") or 0)\n            a = d[\"answers\"]\n            diag = {k: float(a[k][\"noul\"])\n                    for k in (\"completo\", \"restricciones\", \"sin_relleno\")}\n            return {\"p\": float(a[\"cumple\"][\"noul\"]),\n                    \"quality\": float(a[\"calidad\"][\"score\"]),\n                    \"action\": a[\"accion\"][\"choice\"],\n                    \"diag\": diag,\n                    \"fallas\": [k for k, v in diag.items() if v < 0.5]}\n    except Exception:\n        return None\n\n\nTHINK_UP = {\"none\": \"low\", \"low\": \"medium\", \"medium\": \"high\", \"high\": \"high\"}\n\n\nclass Swarm:\n    def __init__(self, task: str, resume_dir: Path | None = None):\n        if len(task.split()) < 4 and not resume_dir:\n            sys.exit(f\"⛔ Reto demasiado vago ({task!r}) — no quemo tokens en eso.\")\n        self.task = task\n        self.run_dir = resume_dir or ROOT / \"runs\" / f\"{datetime.now():%Y%m%d-%H%M%S}-swarm\"\n        self.ship_dir = self.run_dir / \"artifacts\"\n        self.ship_dir.mkdir(parents=True, exist_ok=True)\n        self.results: dict[str, str] = {}\n        self.sem = asyncio.Semaphore(MAX_AGENTS)\n        self.done_events: dict[str, asyncio.Event] = {}\n        self.t0 = time.monotonic()\n        self._beams: set = set()  # POSTs de telemetría en vuelo\n        self._pending_events: list = []  # buffer de telemetría → 1 POST batch\n        self._flush_every = float(os.environ.get(\"SUPABASE_FLUSH_SECONDS\", \"2\"))\n        self._flush_n = int(os.environ.get(\"SUPABASE_FLUSH_EVENTS\", \"20\"))\n        self._last_flush = time.monotonic()\n        # Checkpoint/resume: replan solo si no hay plan previo en el jsonl.\n        self.cached_plan: list | None = None\n        jl = self.run_dir / \"swarm.jsonl\"\n        if resume_dir and jl.exists():\n            for line in jl.read_text().splitlines():\n                try:\n                    ev = json.loads(line)\n                except json.JSONDecodeError:\n                    continue  # línea truncada por un kill: se ignora, no se muere\n                if ev.get(\"event\") == \"plan\":\n                    self.cached_plan = ev[\"tasks\"]\n                    self.task = ev.get(\"task\", task)\n        if resume_dir and self.cached_plan is None:\n            sys.exit(f\"⛔ {jl} no tiene plan checkpointeado — nada que resumir.\")\n\n    def log(self, obj):\n        obj = {\"t\": round(time.time(), 3), **obj}\n        with (self.run_dir / \"swarm.jsonl\").open(\"a\") as f:\n            f.write(json.dumps(obj, ensure_ascii=False) + \"\\n\")\n        self._beam(obj)\n\n    def _beam(self, obj):\n        \"\"\"Telemetría al Army Dashboard (Supabase). Fire-and-forget: un fallo\n        del dashboard JAMÁS toca al enjambre. Buffer en memoria: UN POST batch\n        cada ~2s o al juntar N=20 eventos (Supabase REST acepta array).\"\"\"\n        url = os.environ.get(\"SUPABASE_URL\", \"\").strip()\n        if not url:\n            return\n        try:\n            self._pending_events.append({\n                \"run_id\": self.run_dir.name,\n                \"event\": obj.get(\"event\", \"?\"),\n                \"task_id\": obj.get(\"id\"),\n                \"payload\": obj,\n            })\n            if (len(self._pending_events) >= self._flush_n\n                    or time.monotonic() - self._last_flush >= self._flush_every):\n                self._flush()\n        except Exception:\n            pass\n\n    def _flush(self):\n        \"\"\"Drena el buffer en UN POST batch (array JSON, 1 round-trip).\"\"\"\n        if not self._pending_events:\n            return\n        batch, self._pending_events = self._pending_events, []\n        self._last_flush = time.monotonic()\n        url = os.environ.get(\"SUPABASE_URL\", \"\").strip()\n        if not url:\n            return\n        self._spawn(self._post_batch(url, batch))\n\n    def _spawn(self, coro):\n        try:\n            task = asyncio.get_running_loop().create_task(coro)\n            self._beams.add(task)\n            task.add_done_callback(self._beams.discard)\n        except Exception:\n            coro.close()\n\n    async def _post_batch(self, url, batch):\n        try:\n            import httpx\n\n            async with httpx.AsyncClient(timeout=10) as c:\n                r = await c.post(\n                    f\"{url}/rest/v1/army_events\",\n                    headers={\n                        \"apikey\": os.environ[\"SUPABASE_PUBLISHABLE_KEY\"],\n                        \"Content-Type\": \"application/json\",\n                    },\n                    json=batch,\n                )\n                if r.status_code >= 500 and len(self._pending_events) < 200:\n                    self._pending_events[:0] = batch  # 5xx transitorio: reencolar acotado (4xx = veneno, se suelta)\n        except Exception:\n            if len(self._pending_events) < 200:\n                self._pending_events[:0] = batch\n\n    def ship(self, tid: str, filename: str | None, content: str):\n        \"\"\"Shippear YA: el artefacto toca disco en cuanto existe (escritura atómica).\"\"\"\n        path = self.ship_dir / Path(filename or f\"{tid}.md\").name  # sin rutas del planner\n        tmp = path.with_suffix(path.suffix + \".tmp\")\n        tmp.write_text(content)\n        os.replace(tmp, path)\n        print(f\"  📦 [{time.monotonic()-self.t0:5.1f}s] {tid} shippeado → {path.name}\")\n\n    async def run_task(self, t: dict):\n        # Un fallo en UNA tarea jamás tumba la misión (hallazgo del bug-hunt).\n        try:\n            await self._run_task(t)\n        except RuntimeError as e:\n            self.log({\"event\": \"error\", \"id\": t[\"id\"], \"msg\": repr(e)[:400]})\n            print(f\"  🛑 {e} — abortando misión\")\n            for ev in self.done_events.values():\n                ev.set()\n            self.results.setdefault(t[\"id\"], f\"(abortado: {e})\")\n            return\n        except Exception as e:\n            self.log({\"event\": \"error\", \"id\": t[\"id\"], \"msg\": repr(e)[:400]})\n            print(f\"  💥 {t['id']} falló: {repr(e)[:120]}\")\n            self.results.setdefault(t[\"id\"], f\"(falló: {e})\")\n            self.done_events[t[\"id\"]].set()\n\n    async def _run_task(self, t: dict):\n        # Rama ya shippeada en una corrida anterior: reusar, no re-correr.\n        prior = self.ship_dir / (t.get(\"filename\") or f\"{t['id']}.md\")\n        if prior.exists():\n            self.results[t[\"id\"]] = prior.read_text()\n            print(f\"  ♻️  {t['id']} recuperado del checkpoint ({prior.name})\")\n            self.done_events[t[\"id\"]].set()\n            return\n        for dep in t.get(\"deps\", []):\n            try:\n                await asyncio.wait_for(self.done_events[dep].wait(), timeout=600)\n            except asyncio.TimeoutError:\n                raise RuntimeError(f\"dep {dep} nunca terminó (¿ciclo en el plan?)\")\n        context = \"\\n\\n\".join(\n            f\"[insumo de {d}]\\n{self.results[d][:2000]}\" for d in t.get(\"deps\", [])\n        )\n        prompt = (context + \"\\n\\n\" if context else \"\") + t[\"prompt\"]\n        think = str(t.get(\"thinking\", \"none\")).strip().lower()\n        if think not in THINK_UP:\n            think = \"none\"\n        model = \"deepseek-flash\"\n        warn = False\n        async with self.sem:\n            out = await llm(prompt, system=WORKER_CONSTITUTION, thinking=think)\n            # Escalera de reintentos ENRUTADA POR JEV (máx 2 fixes, luego se\n            # shippea con warn — ship-first, nunca bloquear la misión).\n            for attempt in range(2):\n                # Gate heurístico ANTES de Jev: vacío, rechazo o <80 chars ya\n                # está condenado — no le pagamos una inspección Jev (~12k chars)\n                # a lo que solo puede acabar en fix.\n                if attempt == 0 and not gate(out):\n                    self.log({\"event\": \"jev_skip\", \"id\": t[\"id\"], \"chars\": len(out)})\n                    out = await llm(f\"Este output falló ({out[:300]!r}). Entrega el \"\n                                    f\"resultado completo para: {t['prompt']}\",\n                                    model=model, system=WORKER_CONSTITUTION,\n                                    thinking=think)\n                    continue\n                jv = await jev_review(t[\"prompt\"], out)\n                if jv is None:  # Jev caído → gate heurístico local, sin reintentos ciegos\n                    if not gate(out) and attempt == 0:\n                        out = await llm(f\"Este output falló ({out[:300]!r\n```"}, {"id": "impl_sim", "thinking": "high", "filename": "impl_sim.json", "prompt": "Devuelve SOLO JSON crudo: {\"parches\":[{\"buscar\":\"...\",\"reemplazar\":\"...\"}]}. REGLAS DURAS: buscar = subcadena EXACTA y ÚNICA del código actual (cópiala literal); reemplazar = versión final completa; máx 6 parches quirúrgicos; NO reformatees nada fuera del cambio; conserva estilo y español. Integra las MEJORAS APROBADAS (su campo codigo es la referencia — adáptalo al archivo real).\n\nARCHIVO: dashboard/sim/index.html (zona jevDecide/clasificarPostura/selloHumano) — integra: (1) postura ni_ni en clasificarPostura (criteria nueva: indiferencia genuina, 'ni me va ni me viene') y en el set válido de posturas donde se normaliza; (2) función nueva auditarRespuesta(q,txt,persona) — UNA llamada Jev unificada: postura + fidelidad_personaje (compromiso político) + deseabilidad (score 0-4), muestreada al 20% determinista por hash del texto; loguea a consola por ahora. COLORES ni_ni: usa var(--faint).\n\nMEJORAS APROBADAS:\n[{\"nombre\": \"clasificarPostura v2: cuarta postura ni_ni y set cerrado (fail-closed)\", \"que\": \"El choice postura gana la etiqueta ni_ni (indiferencia genuina: no le interesa el tema, no lo tiene en el radar, no ve diferencia entre las opciones o esquiva sin evaluar) con un criterio que la desambigua de depende (condicion explicita tipo si X entonces Y, o balance real). Conserva la firma clasificarPostura(q,txt) y el tipo de retorno (etiqueta o null); ahora el set es cerrado: cualquier etiqueta fuera de POSTURAS devuelve null en vez de inventar postura, y un texto vacio no gasta llamada. Los callers que ya usaban a_favor/en_contra/depende siguen igual, ni_ni es un cuarto valor.\", \"codigo\": \"const POSTURAS=['a_favor','en_contra','depende','ni_ni'];\\nconst CRIT_POSTURA={\\n  a_favor:'apoya o se inclina claramente a favor',\\n  en_contra:'rechaza o se inclina claramente en contra',\\n  depende:'genuinamente dividida o condicionada: pone una condicion explicita (si X entonces Y), o depende del diseno, del caso o de quien lo proponga',\\n  ni_ni:'indiferencia genuina: no le interesa el tema, no lo tiene en el radar, no ve diferencia entre las opciones o esquiva sin evaluar; SIN condicion y SIN balance'\\n};\\nfunction etiqueta(v,set){v=(typeof v==='string')?v.trim().toLowerCase():null;return set.indexOf(v)>=0?v:null;}\\nfunction payloadPostura(){\\n  return {postura:{type:'choice',\\n    instructions:'Postura que expresa ESTA respuesta sobre el tema de la pregunta',\\n    criteria:CRIT_POSTURA}};\\n}\\nasync function clasificarPostura(q,txt){\\n  if(typeof txt!=='string'||!txt.trim())return null;\\n  const a=await jevDecide('Pregunta: '+q+'\\\\nRespuesta: '+txt,payloadPostura());\\n  return etiqueta(a&&a.postura&&a.postura.choice,POSTURAS); // fail-closed: etiqueta fuera del set cerrado -> null\\n}\\nfunction esNiNi(p){return p==='ni_ni';}\\nfunction cuentaPostura(agg,p){if(POSTURAS.indexOf(p)<0)return agg;agg[p]=(agg[p]||0)+1;return agg;}\", \"impacto\": \"El sim ya puede distinguir indiferencia real de condicionalidad sin romper ningun caller existente ni permitir posturas inventadas.\"}, {\"nombre\": \"check fidelidad_personaje: compromiso politico + lean territorial\", \"que\": \"Nuevo check que valida si una respuesta respeta el compromiso asignado al personaje (indiferente / desencantado / opinador / militante) y su lean territorial, con el payload questions exacto que come jevDecide(state,questions): tres claves tipadas (fidelidad:choice, lean:choice, guion:noul anti-recita-ficha). Las etiquetas validas viven en COMPROMISOS y LEANS_TERR; un perfil raro se degrada a no_declarado/no_aplica en vez de inventar uno, y la salida es fail-closed (etiqueta fuera del set -> null, ok:false y no se pinta).\", \"codigo\": \"const COMPROMISOS=['indiferente','desencantado','opinador','militante'];\\nconst LEANS_TERR=['izquierda','centro','derecha','no_aplica'];\\nconst CRIT_FID={\\n  respeta:'coherente con el compromiso asignado',\\n  rompe:'contradice el compromiso asignado',\\n  neutra:'la respuesta es puro dato o tramite y no revela compromiso: no se puede juzgar'\\n};\\nconst CRIT_LEAN={\\n  respeta:'el encuadre territorial (a quien culpa, a quien beneficia, que zona nombra) coincide con el lean',\\n  rompe:'el encuadre territorial contradice el lean',\\n  no_aplica:'la respuesta no toca nada territorial, o el lean asignado es no_aplica'\\n};\\nfunction payloadFidelidad(perfil){\\n  const comp=(perfil&&COMPROMISOS.indexOf(perfil.compromiso)>=0)?perfil.compromiso:'no_declarado';\\n  const lean=(perfil&&LEANS_TERR.indexOf(perfil.lean)>=0)?perfil.lean:'no_aplica';\\n  return {\\n    fidelidad:{type:'choice',\\n      instructions:'El personaje tiene asignado el compromiso politico '+comp+'. Responde si la RESPUESTA habla como alguien con ese compromiso: indiferente = no le interesa el tema y no lo tiene en el radar; desencantado = habla con desgana y desconfianza hacia la politica; opinador = opina de todo con seguridad, incluso sin datos; militante = defiende activamente una causa o un partido con su vocabulario.',\\n      criteria:CRIT_FID},\\n    lean:{type:'choice',\\n      instructions:'Lean territorial asignado: '+lean+'. Responde si el encuadre territorial de la RESPUESTA lo respeta.',\\n      criteria:CRIT_LEAN},\\n    guion:{type:'noul',\\n      instructions:'Devuelve 1 si la respuesta RECITA la ficha del personaje (muletillas del compromiso puestas a la fuerza) y 0 si suena organica'}\\n  };\\n}\\nasync function fidelidadPersonaje(resp,perfil,state){\\n  const o=(resp&&typeof resp==='object')?resp:{txt:String(resp||'')};\\n  const txt=String(o.txt||'');\\n  if(!txt.trim())return null;\\n  const st=state||('Compromiso asignado: '+((perfil&&perfil.compromiso)||'?')+'\\\\nLean territorial: '+((perfil&&perfil.lean)||'?')+'\\\\nPregunta: '+(o.q||'')+'\\\\nRespuesta: '+txt);\\n  const a=await jevDecide(st,payloadFidelidad(perfil));\\n  if(!a)return null;\\n  const cF=a.fidelidad&&a.fidelidad.choice, cL=a.lean&&a.lean.choice;\\n  const f=['respeta','rompe','neutra'].indexOf(cF)>=0?cF:null;    // fail-closed\\n  const l=['respeta','rompe','no_aplica'].indexOf(cL)>=0?cL:null; // fail-closed\\n  return {fidelidad:f,lean:l,recita:Number(a.guion&&a.guion.noul)===1,ok:(f!==null&&l!==null)};\\n}\", \"impacto\": \"Se detecta al personaje que se sale de su compromiso o de su base territorial, y al que recita la ficha en vez de hablar.\"}, {\"nombre\": \"deseabilidad social: score 0-4 por muestreo determinista del 20%\", \"que\": \"Detector de ciudadano de manual (democrata churchilliano: institucionalista, equilibrado, todos tienen parte de razon, cero tripas) como score 0-4 con CRIT_DESEAB de 5 anclas. Se aplica por muestreo al 20% via hash32(id) determinista: la misma respuesta siempre cae en la misma muestra, asi que nunca se paga dos veces la misma auditoria y el 80% restante no gasta ni una llamada. forzar=true salta el muestreo; score fuera de 0-4 o respuesta no numerica devuelve null (fail-closed).\", \"codigo\": \"const MUESTREO=20; // % de respuestas auditadas\\nfunction hash32(s){let h=2166136261;for(let i=0;i<s.length;i++){h^=s.charCodeAt(i);h=Math.imul(h,16777619);}return h>>>0;}\\nfunction enMuestra(id){return (hash32(String(id))%100)<MUESTREO;} // determinista: misma respuesta, misma decision, un solo cobro\\nconst CRIT_DESEAB=['nada: opinion cruda, con interes propio y sin edulcorar','poco','algo','bastante','mucho: discurso de manual, institucionalista y sin aristas'];\\nfunction payloadDeseabilidad(){\\n  return {deseabilidad:{type:'score',\\n    instructions:'0 = dice lo que piensa aunque suene mal o le perjudique; 4 = suena a ciudadano de manual (democrata churchilliano: institucionalista, equilibrado, todos tienen parte de razon, cero tripas, cero interes propio). Puntua SOLO el tono de correccion, nunca la veracidad.',\\n    criteria:CRIT_DESEAB}};\\n}\\nasync function deseabilidadSocial(resp,forzar){\\n  const o=(resp&&typeof resp==='object')?resp:{txt:String(resp||'')};\\n  const txt=String(o.txt||'');\\n  if(!txt.trim())return null;\\n  const id=o.id||((o.q||'')+'|'+txt);\\n  if(!forzar&&!enMuestra(id))return null; // 80% restante: cero llamadas\\n  const a=await jevDecide('Pregunta: '+(o.q||'')+'\\\\nRespuesta: '+txt,payloadDeseabilidad());\\n  const s=a&&a.deseabilidad&&a.deseabilidad.score;\\n  return (typeof s==='number'&&s>=0&&s<=4)?s:null; // fail-closed\\n}\", \"impacto\": \"Mide el tono de manual a coste acotado (1 llamada por cada 5 respuestas) sin repetir cobros entre recargas.\"}, {\"nombre\": \"auditoria unificada: postura + fidelidad + lean + guion + deseabilidad en 1 sola llamada\", \"que\": \"Sustituye las 3-4 llamadas por respuesta por una sola facturable cada 5 respuestas: Object.assign funde los payloads de las mejoras 1-3 en un unico record questions (postura, fidelidad, lean, guion, deseabilidad) y devuelve un objeto con alertas legibles para la pagina. Requiere las mejoras 1-3 pegadas en el mismo archivo (usan POSTURAS, payloadPostura, payloadFidelidad, payloadDeseabilidad, enMuestra): si alguna etiqueta sale fuera de su set, su campo queda en null y ok pasa a false, de modo que un fallo nunca se pinta como dato bueno.\", \"codigo\": \"// 1 sola llamada facturable por cada 5 respuestas; requiere las mejoras 1-3 en el mismo archivo\\nasync function auditarRespuesta(resp,perfil){\\n  const o=(resp&&typeof resp==='object')?resp:{txt:String(resp||'')};\\n  const txt=String(o.txt||'');\\n  if(!txt.trim())return null;\\n  const id=o.id||((o.q||'')+'|'+txt);\\n  if(!enMuestra(id))return null; // muestreo 20% determinista: aqui esta el ahorro\\n  const questions=Object.assign({},payloadPostura(),payloadFidelidad(perfil),payloadDeseabilidad());\\n  const a=await jevDecide('Compromiso asignado: '+((perfil&&perfil.compromiso)||'?')+\\n    '\\\\nLean territorial: '+((perfil&&perfil.lean)||'?')+\\n    '\\\\nPregunta: '+(o.q||'')+'\\\\nRespuesta: '+txt,questions);\\n  if(!a)return null;\\n  const cP=a.postura&&a.postura.choice, cF=a.fidelidad&&a.fidelidad.choice, cL=a.lean&&a.lean.choice;\\n  const p=POSTURAS.indexOf(cP)>=0?cP:null;                        // fail-closed\\n  const f=['respeta','rompe','neutra'].indexOf(cF)>=0?cF:null;    // fail-closed\\n  const l=['respeta','rompe','no_aplica'].indexOf(cL)>=0?cL:null; // fail-closed\\n  const d=a.deseabilidad&&a.deseabilidad.score;\\n  const out={postura:p,fidelidad:f,lean:l,\\n             recita:Number(a.guion&&a.guion.noul)===1,\\n             deseabilidad:(typeof d==='number'&&d>=0&&d<=4)?d:null};\\n  out.alertas=[];\\n  if(f==='rompe')out.alertas.push('rompe personaje');\\n  if(l==='rompe')out.alertas.push('rompe lean territorial');\\n  if(out.recita)out.alertas.push('recita la ficha');\\n  if(out.deseabilidad>=3)out.alertas.push('deseabilidad social '+out.deseabilidad+'/4');\\n  if(p==='ni_ni')out.alertas.push('ni_ni');\\n  out.ok=(p!==null&&f!==null&&l!==null); // si falta algo no se pinta como valido\\n  return out;\\n}\", \"impacto\": \"Baja el coste por respuesta auditada a una sola llamada y deja la fidelidad, la postura y la deseabilidad en un unico objeto con alertas listas para el panel.\"}]\n\nZONA ACTUAL:\n```\ndataset.rid]){e.preventDefault();verResidente(RESBY[card.dataset.rid]);}\n});\nfunction bubble(r,alt,parent){\n  RESBY[r.id]=r;\n  const host=parent||$(\"#resps\");\n  const card=document.createElement(\"div\");card.className=\"resp\"+(alt?\" alt\":\"\");\n  const cv=document.createElement(\"canvas\");cv.className=\"px\";retrato(cv,r.id,r.sexo,r.ocupacion,r.edad);\n  card.append(cv);\n  card.dataset.rid=r.id;card.title=\"ver ficha DANE\";\n  card.insertAdjacentHTML(\"beforeend\",`<div><div class=\"who\">${esc(r.nombre)} · ${r.edad} ·\n    ${esc(r.ocupacion)}${SEL.size!==1?\" · <span style='color:var(--accent)'>\"+esc(r.dpto_nombre)+\"</span>\":\"\"}</div>\n    <div class=\"say thinking\">está escribiendo</div></div>`);\n  host.append(card);host.scrollTop=1e9;\n  return card;\n}\nlet OPC=null;  // pregunta tipo \"A o B\": {a,b}\nfunction opcionesDe(q){\n  const m=q.match(/^¿?\\s*(.{2,28}?)\\s+o\\s+(.{2,28}?)\\s*\\??$/i);\n  return m?{a:m[1].trim(),b:m[2].trim()}:null;\n}\nfunction etiquetas(){\n  return OPC?[OPC.a,OPC.b,\"ni uno ni otro\"]:[\"a favor\",\"en contra\",\"depende\"];\n}\nfunction tallyHTML(c,extra){\n  const [la,lb,ld]=etiquetas();\n  const cel=(n,l,cls)=>`<div class=\"tal ${cls}\"><b>${n}</b><span>${esc(l)}</span></div>`;\n  return cel(c.a_favor,la,\"fav\")+cel(c.en_contra,lb,\"con\")+cel(c.depende,ld,\"dep\")+(extra||\"\");\n}\nfunction postura(txt){\n  const m=txt.match(/\\[POSTURA:?\\s*(a_favor|en_contra|depende)/i);\n  let limpio=txt.replace(/\\[POSTURA[^\\]]*\\]?/gi,\"\").trim();\n  // si el modelo quedó cortado a mitad de frase, recortar a la última completa\n  if(limpio.length>60&&!/[.!?…\"»)]$/.test(limpio)){\n    const corte=Math.max(limpio.lastIndexOf(\". \"),limpio.lastIndexOf(\"! \"),\n      limpio.lastIndexOf(\"? \"),limpio.lastIndexOf(\"… \"),limpio.lastIndexOf(\".\\n\"));\n    if(corte>limpio.length*0.35)limpio=limpio.slice(0,corte+1);\n    else limpio+=\"…\";   // sin dónde cortar: al menos cerrar con elipsis honesta\n  }\n  return [m?m[1].toLowerCase():null,limpio];\n}\n/* Jev vía backend: decisiones tipadas para el sim (null si no hay backend) */\nasync function jevDecide(state,questions){\n  try{\n    const r=await fetch(\"/api/opina\",{method:\"POST\",headers:{\"Content-Type\":\"application/json\"},\n      body:JSON.stringify({decide:true,state,questions})});\n    if(r.ok)return (await r.json()).answers||null;\n  }catch(e){}\n  return null;\n}\n/* postura robusta: si el residente no cerró la etiqueta, Jev clasifica el texto */\nasync function clasificarPostura(q,txt){\n  const a=await jevDecide(`Pregunta: ${q}\\nRespuesta: ${txt}`,\n    {postura:{type:\"choice\",instructions:\"La postura que expresa esta respuesta\",\n      criteria:{a_favor:\"apoya o se inclina a favor\",en_contra:\"rechaza o se inclina en contra\",\n                depende:\"genuinamente dividida o condicionada\"}}});\n  return a?.postura?.choice||null;\n}\n/* sello de humanidad: Jev califica si la mesa sonó a gente de verdad */\nasync function selloHumano(q,transcript,host){\n  const a=await jevDecide(`Tema: ${q}\\nConversación:\\n`+\n    transcript.map(t=>`${t.n}: ${t.t}`).join(\"\\n\"),\n    {humana:{type:\"noul\",instructions:\"La conversación suena a personas reales distintas (no a un mismo bot repetido): estilos variados, vidas concretas, nada de frases de IA\"},\n     sustancia:{type:\"score\",instructions:\"Sustancia de los argumentos\",\n       criteria:[\"vacía\",\"floja\",\"aceptable\",\"buena\",\"excelente\"]}});\n  if(!a)return;\n  const h=Math.round((a.humana?.noul||0)*100),s=a.sustancia?.score;\n  (host||$(\"#resps\")).insertAdjacentHTML(\"beforeend\",\n    `<div class=\"ronda\" style=\"letter-spacing:.08em\">SELLO HUMANO · humanidad ${h}% · sustancia ${s?.toFixed(1)??\"—\"}/4</div>`);\n}\n/* ── CONTEXTO REAL: una búsqueda web por pregunta; las voces reaccionan a hechos ── */\nlet CTX={q:null,txt:null,ok:false};  // fail-closed: sin verificación tipada de Jev, el contexto NUNCA se rotula como real\n/* CACHE COMPARTIDA (anti-gasto): 1) contexto.json pre-horneado con las sugerencias\n   (contexto() corrido sobre las 5 de cada dpto + \"Toda Colombia\" antes de las 6pm);\n   2) localStorage por pregunta: si alguien ya pagó la búsqueda, los demás la reusan.\n   Un hit devuelve texto SIN disparar ninguna de las 3 llamadas facturables. */\nconst CTX_LS=\"sim-ctx-v1\",CTX_TTL=6*3600*1000;\nlet CTX_ESTATICO={},CTX_STATIC_P=null;\nfunction cargarCtxStatic(){\n  if(!CTX_STATIC_P)CTX_STATIC_P=fetch(\"contexto.json\").then(r=>r.ok?r.json():{})\n    .catch(()=>({})).then(j=>{CTX_ESTATICO=j||{};return CTX_ESTATICO;});\n  return CTX_STATIC_P;\n}\nfunction ctxStatic(q){\n  const k=String(q||\"\").trim().slice(0,400);if(!k)return null;\n  const v=CTX_ESTATICO[k];if(v==null)return null;\n  return typeof v===\"string\"?{txt:v||null,ok:true}:{txt:v.txt||null,ok:v.ok!==false};\n}\nfunction ctxLS(q){\n  const k=String(q||\"\").trim().slice(0,400);if(!k)return null;\n  try{\n    const v=JSON.parse(localStorage.getItem(CTX_LS)||\"{}\")[k];\n    if(v&&Date.now()-(v.t||0)<CTX_TTL)return {txt:v.txt||null,ok:v.ok!==false};\n  }catch(e){}\n  return null;\n}\nfunction ctxGuardarLS(q,txt,ok){\n  const k=String(q||\"\").trim().slice(0,400);if(!k)return;\n  try{\n    const o=JSON.parse(localStorage.getItem(CTX_LS)||\"{}\");\n    o[k]={txt,ok,t:Date.now()};\n    const ks=Object.keys(o);\n    if(ks.length>80)ks.sort((a,b)=>(o[a].t||0)-(o[b].t||0)).slice(0,ks.length-60).forEach(x=>delete o[x]);\n    localStorage.setItem(CTX_LS,JSON.stringify(o));\n  }catch(e){}\n}\n/* memoización por PROMESA: con 33 mesas concurrentes en modo país, todas\n   comparten UNA búsqueda de contexto en vez de disparar 33 idénticas */\nconst CTXP=new Map();\nfunction contexto(q){\n  if(CTXP.has(q))return CTXP.get(q);\n  if(CTXP.size>20)CTXP.clear();\n  const p=contexto1(q).catch(e=>{CTXP.delete(q);throw e;});\n  CTXP.set(q,p);\n  return p;\n}\nasync function contexto1(q){\n  if(CTX.q===q)return CTX.txt;\n  // CACHE COMPARTIDA antes de cualquier llamada facturable: contexto.json pre-horneado\n  // y localStorage por pregunta devuelven el mismo texto a coste cero\n  if(typeof q===\"string\"&&q.trim()&&q.length<=400){\n    await cargarCtxStatic();\n    const hit=ctxStatic(q)||ctxLS(q);\n    if(hit){CTX={q,txt:hit.txt,ok:hit.ok};return CTX.txt;}\n  }\n  CTX={q,txt:null,ok:null};\n  // la rama `online:true` es la reserva más cara (~$0.02): no se dispara sin pregunta\n  // real ni con texto desmedido — el cupo se cobra al recibir, no al responder el 400\n  if(typeof q!==\"string\"||!q.trim()||q.length>400){CTX={q:null,txt:null,ok:null};return null;}\n  try{\n    const SIS=\"HOY es 21 de septiembre de 2026. Interpreta el TEMA como lo entendería un colombiano de a pie (ej: 'Millonarios o Santa Fe' es la rivalidad futbolera de siempre, principalmente fútbol masculino, no solo la noticia del día). Busca noticias recientes de Colombia y devuelve SOLO 2-3 viñetas ULTRA CORTAS (máx 16 palabras) con hechos EXPLÍCITOS de fuentes, cada una cerrando con el medio entre paréntesis, ej (El Tiempo) — JAMÁS URLs. Si un dato no cuadra con la fecha, exclúyelo. Empieza directo con '- '. PROHIBIDO opinar o presentarte. Sin nada: SIN_NOVEDADES.\";\n    const pide=u=>fetch(\"/api/opina\",{method:\"POST\",headers:{\"Content-Type\":\"application/json\"},\n        body:JSON.stringify({online:true,max_tokens:260,messages:[\n          {role:\"system\",content:SIS},{role:\"user\",content:u}]})})\n      .then(r=>r.ok?r.json():null).then(j=>j?.content||\"\").catch(()=>\"\");\n    // CINCO búsquedas en paralelo, cada una con su lente\n    const terr=SEL.size&&SEL.size<=4?[...SEL].map(c=>MARG[c]?.nombre).filter(Boolean).join(\", \"):null;\n    const brotes=await Promise.all([\n      pide(\"TEMA: \"+q+\" — la noticia del momento (Colombia, septiembre 2026)\"),\n      pide(\"TEMA: cifras y datos duros sobre: \"+q+\" (Colombia: precios, porcentajes, fechas)\"),\n      pide(\"TEMA: qué dice la gente de a pie y las redes sobre: \"+q+\" (Colombia: quejas, memes, sentir popular)\"),\n      pide(\"TEMA: el fondo: qué ha pasado antes en Colombia con: \"+q+\" (historia corta, antecedentes)\"),\n      pide(terr?(\"TEMA: cómo toca esto a \"+terr+\": \"+q):(\"TEMA: diferencias regionales en Colombia sobre: \"+q)),\n    ]);\n    const c1=brotes.join(\"\\n\"),c2=\"\";\n    const norm=s=>s.toLowerCase().normalize(\"NFD\").replace(/[^a-z0-9 ]/g,\"\").slice(0,52);\n    const vistos=new Set(),vi=[];\n    for(const s of (c1+\"\\n\"+c2).split(\"\\n\").map(x=>x.trim())){\n      if(!s.startsWith(\"- \")||/SIN_NOVEDADES/.test(s))continue;\n      const k=norm(s);\n      if([...vistos].some(v=>v.startsWith(k.slice(0,34))||k.startsWith(v.slice(0,34))))continue;\n      vistos.add(k);vi.push(s);\n      if(vi.length>=6)break;\n    }\n    CTX.txt=vi.length?vi.join(\"\\n\"):null;\n    /* Jev verifica el contexto antes de mostrarlo (fechas/consistencia) */\n    if(CTX.txt){\n      try{\n        const v=await fetch(\"/api/opina\",{method:\"POST\",headers:{\"Content-Type\":\"application/json\"},\n          body:JSON.stringify({decide:true,\n            state:\"HOY: 21 septiembre 2026. Resumen noticioso a verificar:\\n\"+CTX.txt,\n            questions:{fiable:{type:\"noul\",instructions:\n              \"Las viñetas son internamente consistentes, coherentes con la fecha de hoy (sin cargos o mandatos vencidos presentados como vigentes, sin fechas imposibles) y suenan a hechos citados, no a invención\"}}})});\n        if(v.ok){const j=await v.json();\n          const n=j?.answers?.fiable?.noul;\n          // sin `answers.fiable` tipado no hay verificación: el contexto queda NO verificado\n          CTX.ok=typeof n===\"number\"&&n>=0.5;}\n      }catch(e){}\n    }\n  }catch(e){}\n  ctxGuardarLS(q,CTX.txt,CTX.ok);\n  return CTX.txt;\n}\nfunction limpiaCtx(s){\n  return s.replace(/\\[([^\\]]*)\\]\\((https?:[^)]*)\\)/g,\"$1\")   // [txt](url) → txt\n          .replace(/https?:\\/\\/\\S+/g,\"\").replace(/\\(\\s*\\)/g,\"\").trim();\n}\nfunction ctxBox(txt,host){\n  if(txt)txt=limpiaCtx(txt);\n  const dudas=CTX.ok===false;\n  (host||$(\"#resps\")).insertAdjacentHTML(\"beforeend\",\n    txt?`<div class=\"ctx${dudas?\" off\":\"\"}\" style=\"${dudas?\"border-color:var(--fail)\":\"\"}\"><b style=\"${dudas?\"color:var(--fail)\":\"\"}\">${dudas?\"⚠ CONTEXTO SIN VERIFICAR — con pinzas\":\"📰 LO QUE ESTÁ PASANDO (búsqueda verificada)\"}</b><span class=\"cbody\">${esc(txt)}</span></div>`\n       :`<div class=\"ctx off\"><b>📰 SIN NOTICIAS FRESCAS</b><span class=\"cbody\">opinan desde su experiencia, no desde la prensa</span></div>`);\n}\nconst ctxPara=txt=>(txt&&CTX.ok!==false)?`\\nNOTICIAS RECIENTES (por si tu personaje anda enterado — NO estás obligado a citarlas: la mayoría opina desde su vida. Si el tema es de siempre —fútbol, comida, política de fondo— habla de lo de SIEMPRE, no solo de la noticia del día):\\n${txt}\\n`:\"\";\n\nlet API_OK=null;  // ¿existe /api/opina? (en Vercel sí; en local http.server no)\nasync function flash(messages,max=170,temp=0.95){\n  // validar ANTES de cualquier fetch facturable: el cupo del backend se carga al\n  // RECIBIR la petición, así que un `messages` vacío/malformado (o desmedido, p.ej.\n  // 41 turnos) quema reserva y luego responde 400 sin coste real\n  if(!Array.isArray(messages)||!messages.length||messages.length>16||\n     messages.some(m=>!m||typeof m.content!==\"string\"||!m.content.trim()))\n    throw new Error(\"messages inválidos: no se dispara ningún fetch facturable\");\n  if(API_OK!==false){\n    try{\n      const r=await fetch(\"/api/opina\",{method:\"POST\",\n        headers:{\"Content-Type\":\"application/json\"},\n        body:JSON.stringify({messages,max_tokens:max,temperature:temp})});\n      if(r.ok){API_OK=true;return (await r.json()).content||\"\";}\n      // el backend EXISTE y contestó con error (400/5xx/timeout): su reserva de cupo ya\n      // se cargó, así que NO se reintenta contra OpenRouter (pagaríamos dos veces el intento)\n      if(r.status===404||r.status===405||r.status===501){API_OK=false;}  // 501: http.server local sin POST\n      else{API_OK=true;throw new Error(\"api \"+r.status);}\n    }catch(e){if(API_OK)throw e;API_OK=false;}\n  }\n  // respaldo local: key del navegador (ajustes o local_keys)\n  if(!cfg.orkey)throw new Error(\"sin backend ni key\");\n  const r=await fetch(\"https://openrouter.ai/api/v1/chat/completions\",{\n    method:\"POST\",headers:{Authorization:\"Bearer \"+cfg.orkey,\"Content-Type\":\"application/json\"},\n    body:JSON.stringify({model:\"deepseek/deepseek-v4.1-flash\",max_tokens:max,\n      temperature:temp,reasoning:{enabled:false},messages})});\n  const j=await r.json();\n  if(j.error)throw new Error(j.error.message||\"api\");\n  return j.choices?.[0]?.message?.content||\"\";\n}\nfunction tallyVivo(postDe,n){\n  const c={a_favor:0,en_contra:0,depende:0};\n  Object.values(postDe).forEach(v=>c[v]++);\n  const falta=n-Object.keys(postDe).length;\n  $(\"#tally\").innerHTML=tallyHTML(c,\n    falta>0?`<div class=\"tal\"><b>${falta}</b><span>por hablar</span></div>`:\"\");\n}\nfunction tally(p){\n  $(\"#tally\").innerHTML=tallyHTML(p);\n}\nfunction muestraDe(sel,n){\n  // muestra de los territorios elegidos (o todo el país), proporcional a población REAL\n  const codigos=sel.size?[...sel]:Object.keys(MARG);\n  const pesos=codigos.map(c=>[c,MARG[c]?.poblacion||1]);\n  const tot=pesos.reduce((a,[,w])=>a+w,0);\n  const out=[],usados=new Set();\n  let guardia=0;\n  while(out.length<n&&guardia++<n*200){\n    let x=Math.random()*tot,cod=pesos[0][0];\n    for(const [c,w] of pesos){x-=w;if(x<=0){cod=c;break}}\n    const pool=RES.filter(r=>r.dpto===cod&&r.edad>=16&&!usados.has(r.id));\n    if(pool.length){const r=pool[Math.floor(Math.random()*pool.length)];\n      usados.add(r.id);out.push(r);}\n  }\n  return out;\n}\n/* una corrida preemptada deja su UI colgada: ctrl visible, reloj corriendo y\n   PREGUNTAR deshabilitado. abortar() la deja en reposo, pero solo si esta\n   corrida sigue siendo dueña de CONV — si otra ya tomó el control, tocarla\n   mataría su reloj y habilitaría su botón a media tertulia. */\nfunction abortar(C){\n  if(C&&CONV!==C)return;\n  $(\"#ctrl\").style.display=\"none\";$(\"#reloj\").textContent=\"\";\n  const b=$(\"#qbar button\");if(b)b.disabled=false;\n}\nasync function conversar(q){\n  // al entrar: limpia la UI de cualquier corrida preemptada ANTES del dispatch\n  // de modo — en PAÍS se sale temprano y nada más la tocaría.\n  abortar(null);\n  $(\"#btnpausa\").textContent=\"⏸ PAUSAR\";$(\"#btnpausa\").classList.remove(\"pausa\");\n```"}]}